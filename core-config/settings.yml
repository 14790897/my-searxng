# Read the documentation before extending the defaults:
# https://docs.searxng.org/admin/settings/

use_default_settings: true

server:
  secret_key: ""
  image_proxy: true
  # limiter: false  # 本地自用内网关闭访问限流，公网实例建议true
search:
  formats:
    - html
    - json  # 这一行必须存在，否则无法返回JSON API数据
